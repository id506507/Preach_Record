import 'package:flutter/material.dart';

class ReturnVisit extends StatelessWidget {
  const ReturnVisit({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Return Visit'),
    );
  }
}
